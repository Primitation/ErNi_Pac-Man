[tool.mypy]
exclude = "^(\\.venv|minilibx-linux)/"
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mazegen", "mazegen.*"]

[project]
name = "Pac-man"
version = "1.0.0"
description = "Pac-Man Game"
authors = [
    {name = "nbreton"},
    {name = "erxia"}
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    
]